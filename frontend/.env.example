# Copy to .env (gitignored) to override locally.

# Next.js dev server port. Read by `next dev` and `next start`.
PORT=3001

# Backend API base URL. Must be prefixed with NEXT_PUBLIC_ to be exposed
# to the browser. Matches APP_PORT in backend's app/config.py.
NEXT_PUBLIC_API_URL=http://localhost:9000
